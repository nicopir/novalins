<%= form_for [@project, @input_file] do |f| %>
  <% if @input_file.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@input_file.errors.count, "error") %> prohibited this input_file from being saved:</h2>
      <ul>
      <% @input_file.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :file_path %><br>
    <%= f.file_field :file_path %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
